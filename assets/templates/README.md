# Templates

This directory is reserved for report templates used by the CLI's `report` command.

Currently, reports are rendered programmatically from CTRF JSON data. If you create custom HTML templates, place them here.
